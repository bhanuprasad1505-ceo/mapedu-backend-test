import {useEffect,useState} from 'react';import{api}from'../services/api';
export default function Cards(){const[a,setA]=useState<any[]>([]);const[show,setShow]=useState(false);const load=()=>api.cards('SCH001').then(setA).catch(()=>setA([]));useEffect(load,[]);return <section className="panel"><div className="toolbar"><button onClick={()=>setShow(!show)}>+ Register RFID Card</button></div>{show&&<form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.createCard({cardUid:f.get('uid'),personType:f.get('type'),personId:f.get('person'),schoolCode:'SCH001'});setShow(false);load()}} className="form"><input name="uid" placeholder="3585CBCA" required/><select name="type"><option>STUDENT</option><option>EMPLOYEE</option></select><input name="person" placeholder="STU000001" required/><button>Register</button></form>}<table><thead><tr><th>UID</th><th>Type</th><th>Person</th><th>Status</th></tr></thead><tbody>{a.map(x=><tr key={x.cardUid}><td>{x.cardUid}</td><td>{x.personType}</td><td>{x.personId}</td><td>{x.active?'Active':'Inactive'}</td></tr>)}</tbody></table></section>}