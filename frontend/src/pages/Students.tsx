import {useEffect,useState} from 'react';import{api}from'../services/api';
export default function Students(){const[a,setA]=useState<any[]>([]);const[show,setShow]=useState(false);const load=()=>api.students('SCH001').then(setA).catch(()=>setA([]));useEffect(load,[]);return <section className="panel"><div className="toolbar"><button onClick={()=>setShow(!show)}>+ Add Student</button></div>{show&&<form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.createStudent({studentId:f.get('id'),schoolCode:'SCH001',name:f.get('name'),className:f.get('class'),section:f.get('section')});setShow(false);load()}} className="form"><input name="id" placeholder="STU000001" required/><input name="name" placeholder="Name" required/><input name="class" placeholder="Class"/><input name="section" placeholder="Section"/><button>Save</button></form>}<table><thead><tr><th>ID</th><th>Name</th><th>Class</th><th>Section</th><th>Status</th></tr></thead><tbody>{a.map(x=><tr key={x.studentId}><td>{x.studentId}</td><td>{x.name}</td><td>{x.className}</td><td>{x.section}</td><td>{x.active?'Active':'Inactive'}</td></tr>)}</tbody></table></section>}