import {useEffect,useState} from 'react';import{api}from'../services/api';
export default function Employees(){const[a,setA]=useState<any[]>([]);const[show,setShow]=useState(false);const load=()=>api.employees('SCH001').then(setA).catch(()=>setA([]));useEffect(load,[]);return <section className="panel"><div className="toolbar"><button onClick={()=>setShow(!show)}>+ Add Employee</button></div>{show&&<form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.createEmployee({employeeId:f.get('id'),schoolCode:'SCH001',name:f.get('name'),department:f.get('department')});setShow(false);load()}} className="form"><input name="id" placeholder="EMP000001" required/><input name="name" placeholder="Name" required/><input name="department" placeholder="Department"/><button>Save</button></form>}<table><thead><tr><th>ID</th><th>Name</th><th>Department</th><th>Status</th></tr></thead><tbody>{a.map(x=><tr key={x.employeeId}><td>{x.employeeId}</td><td>{x.name}</td><td>{x.department}</td><td>{x.active?'Active':'Inactive'}</td></tr>)}</tbody></table></section>}