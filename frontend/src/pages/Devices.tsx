import {useEffect,useState} from 'react';import{api}from'../services/api';
export default function Devices(){const[a,setA]=useState<any[]>([]);const[show,setShow]=useState(false);const load=()=>api.devices('SCH001').then(setA).catch(()=>setA([]));useEffect(load,[]);return <section className="panel"><div className="toolbar"><button onClick={()=>setShow(!show)}>+ Register Device</button></div>{show&&<form onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);await api.createDevice({deviceId:f.get('id'),deviceName:f.get('name'),deviceType:f.get('type'),schoolCode:'SCH001',location:f.get('location'),firmwareVersion:f.get('firmware')});setShow(false);load()}} className="form"><input name="id" placeholder="DEVICE001" required/><input name="name" placeholder="MAPEDU RFID Reader" required/><input name="type" value="MAPEDU-RFID" readOnly/><input name="location" placeholder="Classroom"/><input name="firmware" value="V1.1.0" readOnly/><button>Register</button></form>}<table><thead><tr><th>ID</th><th>Name</th><th>Location</th><th>Firmware</th><th>Last Seen</th><th>Status</th></tr></thead><tbody>{a.map(x=><tr key={x.deviceId}><td>{x.deviceId}</td><td>{x.deviceName}</td><td>{x.location}</td><td>{x.firmwareVersion}</td><td>{x.lastSeen||'-'}</td><td>{x.active?'Active':'Inactive'}</td></tr>)}</tbody></table></section>}