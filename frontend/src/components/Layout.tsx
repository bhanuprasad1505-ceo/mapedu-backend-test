import React from 'react';
type Props={page:string;setPage:(p:string)=>void;children:React.ReactNode};
export default function Layout({page,setPage,children}:Props){const items=[['dashboard','Dashboard'],['students','Students'],['employees','Employees'],['cards','RFID Cards'],['devices','Devices'],['attendance','Attendance']];return <div className="layout"><aside className="sidebar"><div className="logo">MAPEDU</div><div className="nav">{items.map(([id,label])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}>{label}</button>)}</div></aside><main className="main"><header><div><h1>{items.find(x=>x[0]===page)?.[1]}</h1><span>School: SCH001</span></div><button className="refresh" onClick={()=>window.location.reload()}>Refresh</button></header>{children}</main></div>}